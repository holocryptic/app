<h1>Editing post</h1>

<%= render 'form' %>


<%= link_to 'Show', "/books/#{@book.id}/posts/#{@post.id}" %> 
<%= link_to 'Back', book_post_path(@book, @post) %>
<%= link_to 'Destroy', "/books/#{@book.id}/posts/#{@post.id}", :method => :delete %>
